name: Run RSS Scout

on:
  schedule:
    - cron: '0 18 * * 6' # Каждую субботу в 10:00 UTC
  workflow_dispatch: # Кнопка для ручного запуска

permissions:
  contents: write

jobs:
  run-scout:
    runs-on: ubuntu-latest
    steps:
      - name: Скачивание кода
        uses: actions/checkout@v4

      - name: Установка Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Установка библиотек
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 feedparser

      - name: Запуск Разведчика
        # Здесь мы передаем секреты в наш Python-скрипт
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
          SERPER_API_KEY: ${{ secrets.SERPER_API_KEY }}
        run: python scout.py
      
      - name: Сохранение карантинных ссылок
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add pending_sources.txt source_links.txt
          git commit -m "Разведка: добавлены новые потенциальные источники" || echo "Нет новых источников"
          git push
